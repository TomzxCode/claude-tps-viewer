[project]
name = "claude-tps-viewer"
version = "0.1.0"
description = "Visualize Claude Code tokens per second through time."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "mkdocs>=1.5.0",
    "mkdocs-material[imaging]>=9.4.0",
]
